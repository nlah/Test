# Patch generated by Pyment v0.3.2

--- a/celery_my\tasks.py
+++ b/celery_my\tasks.py
@@ -6,9 +6,7 @@
 from . celery_app import app
 @app.task
 def add():
-    """
-    Update the database on upc
-    """
+    """Update the database on upc"""
     wolmart = UpcWolmart(app.conf['CELERY_WOLMART_KEY'])
     client = MongoClient(app.conf['MONGO_URI'])
     wolmart.update(client[app.conf['MONGODB_DB']])
